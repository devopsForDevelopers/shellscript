#!/bin/bash

echo "This is my first shell script"

#date command in Linux is used to display current date and time
date

#uptime command in Linux is a powerful tool that displays the current time, the system's uptime(how long the system has been running), the number of users, and the load average.
uptime